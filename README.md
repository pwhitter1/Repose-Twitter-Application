# Repose-Twitter-Application
Adapted from online tutorial - Uses Twitter’s API to grab tweets that include keywords that indicate dispiritedness and automatically “tweets at” the user words of comfort
